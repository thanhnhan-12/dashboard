export enum ETypeAButtonPC {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  GHOST = 'ghost',
  ACCENT = 'accent',
}

export enum ETypeInputField {
  DEFAULT = 'default',
  LEFT = 'left',
  RIGHT = 'right',
  SUFFIX = 'suffix',
}

export enum ETypeCard {
  VERTICAL = 'vertical',
  HORIZONTAL = 'horizontal',
}

export enum ETypeFieldForm {
  TEXT_FIELD = 'TEXT_FIELD',
  TEXT_AREA = 'TEXT_AREA',
  INPUT_NUMBER = 'INPUT_NUMBER',
  PASSWORD = 'PASSWORD',
  SELECT = 'SELECT',
  RADIO = 'RADIO',
  CHECKBOX = 'CHECKBOX',
  RANGE_DATE = 'RANGE_DATE',
  DATEPICKER = 'DATEPICKER',
  UPLOAD = 'UPLOAD',
}

export enum EActiveField {
  ACTIVE = 1,
  INACTIVE = 0,
}

export enum EMessageErrorRequired {
  INPUT = 'input',
  SELECT = 'select',
}

export enum EGroupFormOption {
  応募した企業から連絡がない = 1,
  サイトの使い方について,
  求人の内容について,
  グルスタボーナスについて,
}
