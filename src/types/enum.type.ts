export enum ETypeAButtonPC {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  GHOST = 'ghost',
  ACCENT = 'accent',
}

export enum ETypeInputField {
  DEFAULT = 'default',
  LEFT = 'left',
  RIGHT = 'right',
  SUFFIX = 'suffix',
}

export enum ETypeCard {
  VERTICAL = 'vertical',
  HORIZONTAL = 'horizontal',
}
